// Dependencies
import { FunctionComponent } from "react";

// Styles
import { Loader, LoaderElement } from "./loader.styles";

export const LoaderComponent: FunctionComponent = () => {
    return (
        <Loader><LoaderElement/></Loader>
    );
}