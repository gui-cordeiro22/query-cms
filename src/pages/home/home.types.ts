export type RestaurantListDataTypes = {
    id: number;
    Name: string;
    Description: string;
    createdAt: string;
    updatedAt: string;
 }[];

export type SectionDataTypes = {
    id: number;
    Title: string;
    Subtitle: string;
    Description: string;
}